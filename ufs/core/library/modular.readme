ASLIB HAS GONE MODULAR!

ABSTARCT:
	aslib will still be sourced by init.sh
	aslib will source all files inside core/library/ with a file name template func."functioname".aslib
	all variables must be defined in aslib and exported.
	check logs /logs/ufs for debugging.
WARNING:
	Add a return 0 at the end of your script if your running commands outside of your functions.
	if an error value is detected during sourcing your script will not be loaded.
	
INST:
	USE func._TEMPLATE.aslib for guide